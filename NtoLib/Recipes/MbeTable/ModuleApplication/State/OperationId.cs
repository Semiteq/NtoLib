namespace NtoLib.Recipes.MbeTable.ModuleApplication.State;

// Canonical operation identifiers used by policy and UI.
public enum OperationId
{
    Load,
    Save,
    Send,
    Receive,
    AddStep,
    RemoveStep,
    EditCell
}